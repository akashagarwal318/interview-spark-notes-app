# Copy this file to .env and fill in values. Do NOT commit secrets to git.
# Example: cp .env.example .env

# MongoDB Atlas connection string (replace <USER> and <PASSWORD>)
MONGODB_URI="mongodb+srv://<USER>:<PASSWORD>@cluster0.abcd.mongodb.net/interview-assistant?retryWrites=true&w=majority"

# Server port
PORT=5000

# Node environment (development|production)
NODE_ENV=development
